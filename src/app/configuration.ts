import { domain } from '../environments/environment';

export const grupoclave = {
    subdominios : "1",
    tipocomponente : "2"
}

export const TOKEN_NAME = "token_user";

export const endpoint_evento = `${domain}evento`;
export const endpoint_clavevalor = `${domain}clavevalor`
export const endpoint_grupoclave = `${domain}grupoclave`
export const endpoint_componente = `${domain}componente`
export const endpoint_usuario = `${domain}usuario`
export const endpoint_login = `${domain}login`